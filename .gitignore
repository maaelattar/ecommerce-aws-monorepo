# === Node & Package Managers ===
/node_modules
**/node_modules

# pnpm store (if you ever pin a custom store)
/.pnpm-store

# Lockfiles (only one lockfile per project)
/package-lock.json
/yarn.lock

# === Nx & Build Caches ===
# Nx’s computation cache
/.nx
# Generic build cache
/.cache

# Compiled output
/dist
**/dist
apps/*/dist
libs/*/dist

# === CDK ===
# Cloud assembly
/infra/cdk/cdk.out
/cdk.out

# === TypeScript ===
*.tsbuildinfo

# === Environment & Secrets ===
.env
.env.*.local
secrets.local.json

# === Logs & Reports ===
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*
/logs
*.log

# === Coverage ===
/coverage

# === IDEs & Editors ===
.vscode/
.idea/
*.code-workspace

# === OS Files ===
.DS_Store
Thumbs.db

# === Docker ===
docker-compose.override.yml
docker-compose.override.yaml

# === Misc ===
*.local
